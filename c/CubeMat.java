package c;
/* Another implementation of the Cube class. Hopefully this is faster, and it should also help when we move to abstract
 * coordinates. This stores a permutation class which, rather than being a list of integers, is a boolean matrix which
 * is a list of longs. Matrix multiplication is done (fast, hopefully) as represented by the bitwise & operator.
 */
public class CubeMat {
    
    
    
}
